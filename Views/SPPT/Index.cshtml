@model IEnumerable<SIP.Models.Spop>

@{
    ViewBag.Title = "SPPT";
    ViewBag.SubHeaderTitle = "SPPT";
    ViewBag.S1 = "";
    ViewBag.S2 = "";
    ViewBag.S3 = "";
    ViewBag.S4 = "";
}

<div class="row justify-content-center">
    <div class="col-lg-12">
        <div class="kt-portlet" data-ktportlet="true" id="kt_portlet_tools_1">
            <div class="kt-portlet__head">
                <div class="kt-portlet__head-label">
                    <h3 class="kt-portlet__head-title">
                        @ViewBag.Title
                    </h3>
                </div>
                <div class="kt-portlet__head-toolbar">
                    <div class="kt-portlet__head-group">
                        <a href="#" data-ktportlet-tool="toggle" class="btn btn-sm btn-icon btn-clean btn-icon-md"><i class="la la-angle-down"></i></a>
                    </div>
                </div>
            </div>
            <div class="kt-portlet__body">
                <form asp-action="SPPTMasal" asp-controller="Cetak">
                    <div class="kt-form kt-form--label-right kt-margin-t-10 kt-margin-b-10 kt-margin-5-mobile">
                        <div class="row align-items-center">
                            <div class="col-xl-12">
                                <div class="row align-items-center">
                                    <div class="col-sm-2 col-1 kt-margin-b-10-mobile order-2 order-sm-1 length">
                                        <div class="kt-form__group">
                                            <select class="form-control kt-selectpicker" id="length">
                                                <option value="10">10</option>
                                                <option value="20">20</option>
                                                <option value="30">30</option>
                                                <option value="50">50</option>
                                                <option value="100">100</option>
                                            </select>
                                        </div>
                                    </div>
                                    <div class="col-xl-2 col-lg-2 col-md-3 col-sm-3 col-auto order-3 order-sm-2 kt-margin-b-10-mobile">
                                        <div class="kt-input-icon kt-input-icon--left">
                                            <input type="text" class="form-control" placeholder="Search..." id="search">
                                            <span class="kt-input-icon__icon kt-input-icon__icon--left">
                                                <span><i class="la la-search"></i></span>
                                            </span>
                                        </div>
                                    </div>
                                    <div class="col-xl-3 col-lg-3 col-md-4 col-sm-3 col-12 order-1 order-sm-3 kt-margin-b-10-mobile">
                                        <div class="kt-form__group kt-form__group--inline">
                                            <div class="kt-form__label">
                                                <label>Kecamatan:</label>
                                            </div>
                                            <div class="kt-form__control">
                                                <select class="form-control kt-selectpicker" data-size="5" id="IndKecamatanId">
                                                    <option value=" ">Semua</option>
                                                    @foreach (var items in ViewBag.Kecamatan)
                                                    {
                                                        <option value="@items.IndKecamatanId">@items.Kecamatan</option>
                                                    }
                                                </select>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-xl-3 col-lg-3 col-md-4 col-sm-3 col-12 order-1 order-sm-3 kt-margin-b-10-mobile kt-margin-t-10-tablet">
                                        <div class="kt-form__group kt-form__group--inline">
                                            <div class="kt-form__label">
                                                <label>Kelurahan:</label>
                                            </div>
                                            <div class="kt-form__control">
                                                <select class="form-control kt-selectpicker" disabled data-size="5" id="IndKelurahanId">
                                                    <option value=" ">Semua</option>
                                                </select>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-xl-2 col-lg-2 col-md-3 col-sm-3 col-12 order-1 order-sm-3 kt-margin-b-10-mobile kt-margin-t-10-tablet">
                                        <div class="kt-form__group kt-form__group--inline">
                                            <div class="kt-form__label">
                                                <label>Status:</label>
                                            </div>
                                            <div class="kt-form__control">
                                                <select class="form-control kt-selectpicker" id="status">
                                                    <option value=" ">Semua</option>
                                                    <option value="true">Lunas</option>
                                                    <option value="false">Belum Bayar</option>
                                                </select>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="kt-separator kt-separator--md"></div>
                        <div class="row">
                            <div class="col-lg-12">
                                <button type="submit" class="btn btn-info btn-brand--icon">
                                    <span>
                                        <i class="la la-print"></i>
                                        <span>Cetak</span>
                                    </span>
                                </button>
                            </div>
                        </div>
                    </div>
                    <table class="table table-striped table-hover dt-custom" id="m_table">
                        <thead>
                            <tr>
                                <th width="15">
                                    <label class="kt-checkbox kt-checkbox--tick kt-checkbox--success" style="margin-bottom: 15px!important">
                                        <input type="checkbox" id="checkBoxAll" class="custom-checkbox" />
                                        <span></span>
                                    </label>
                                </th>
                                <th>
                                    Nama WP
                                </th>
                                <th>
                                    No Objek Pajak
                                </th>
                                <th>
                                    Tahun
                                </th>
                                <th>
                                    Letak
                                </th>
                                <th>
                                    Kecamatan
                                </th>
                                <th>
                                    Kelurahan
                                </th>
                                <th>
                                    NJOP
                                </th>
                                <th>
                                    Terhutang
                                </th>
                                <th>
                                    Status
                                </th>
                                <th>
                                    Kelola
                                </th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </form>
            </div>
        </div>
        <!--end::Portlet-->
    </div>
</div>

@section Scripts {
    <script>

        $(document).ready(function () {
            $('#checkBoxAll').click(function () {
                if ($(this).is(":checked")) {
                    $(".chkCheckBoxId").prop("checked", true)
                }
                else {
                    $(".chkCheckBoxId").prop("checked", false)
                }
            });
        });

        var DatatablesAjaxServer = function () {

            var initTable1 = function () {
                var table = $('#m_table');

                // begin first table
                table.DataTable({
                    scrollY: '60vh',
                    scrollX: true,
                    scrollCollapse: true,
                    searchDelay: 500,
                    processing: true,
                    serverSide: true,
                    searching: true,
                    bJQueryUI: true,
                    bFilter: true,
                    bSort: true,
                    dom: "<'row'<'col-sm-12'tr>>\n\t\t\t<'row'<'col-sm-12 col-md-5'i><'col-sm-12 col-md-7'p>>",
                    "ajaxSource": "/SPPT/GetSPPT",
                    fnServerData: function (sSource, aoData, fnCallback) {
                        aoData.push({ "name": "all", "value": true });
                        aoData.push({ "name": "tipe", "value": $('#IndKecamatanId').val() });
                        aoData.push({ "name": "tipe2", "value": $('#IndKelurahanId').val() });
                        aoData.push({ "name": "status", "value": $('#status').val() });
                        $.getJSON(sSource, aoData, function (json) {
                            fnCallback(json)
                        });
                    },
                    columns: [
                        { data: 'id' },
                        { data: 'nama' },
                        { data: 'nop' },
                        { data: 'tahun' },
                        { data: 'lokasi' },
                        { data: 'kecamatan' },
                        { data: 'kelurahan' },
                        { data: 'njop' },
                        { data: 'hutang' },
                        { data: 'status' },
                        { data: 'id' },
                    ],
                    columnDefs: [{
                        targets: 0,
                        width: 15,
                        orderable: false,
                        data: 'id',
                        render: function (data, type, full, meta) {
                            return `<label class="kt-checkbox kt-checkbox--tick kt-checkbox--success" style="margin-bottom: 15px!important">
                                            <input type="checkbox" name="ID" value="` + data + `" class="kt-checkbox kt-checkbox--single kt-checkbox--solid chkCheckBoxId" />
                                            <span></span>
                                        </label>`;
                        }
                    }, {
                        targets: -1,
                        title: 'Kelola',
                        data: 'id',
                        orderable: false,
                        render: function (data, type, full, meta) {
                            if (full['status'] == "1") {
                                return `
                            <span style="overflow: visible; position: relative;">
                                            <span class="dropdown">
                                                <a href="#" class="btn btn-sm btn-label-success" data-toggle="dropdown" aria-expanded="false"> Kelola</a>
                                                <span class="dropdown-menu dropdown-menu-right">
                                                    <a target="_blank" href="/PBB/SPPT/` + full['ido'] + `" class="dropdown-item"><i class="la la-external-link-square"></i> Data SPPT</a>
                                                </span>
                                            </span>
                                        </span>`;
                            }
                            else {
                                return `
                            <span style="overflow: visible; position: relative;">
                                            <span class="dropdown">
                                                <a href="#" class="btn btn-sm btn-label-success" data-toggle="dropdown" aria-expanded="false"> Kelola</a>
                                                <span class="dropdown-menu dropdown-menu-right">
                                                    <a target="_blank" href="/PBB/SPPT/` + full['ido'] + `" class="dropdown-item"><i class="la la-external-link-square"></i> Data SPPT</a>
                                                    <a target="_blank" href="/PBB/STTS/` + full['ids'] + `" class="dropdown-item"><i class="la la-money"></i> Buat STTS</a>
                                                </span>
                                            </span>
                                        </span>`;
                            }
                        }
                    }, {
                        targets: 9,
                        render: function (data, type, full, meta) {
                            var status = {
                                "0": {
                                    'title': 'Belum Bayar',
                                    'state': 'danger'
                                },
                                "1": {
                                    'title': 'Lunas',
                                    'state': 'success'
                                }
                            };
                            if (typeof status[data] === 'undefined') {
                                return data;
                            }
                            return '<span class="kt-badge kt-badge--' + status[data].state + ' kt-badge--dot"></span>&nbsp;' +
                                '<span class="kt-font-bold kt-font-' + status[data].state + '">' + status[data].title + '</span>';
                        }
                    }]
                });

                $('#length').on('change', function () {
                    var selectedValue = $(this).val();
                    table.DataTable().page.len(selectedValue).draw();
                });

                $('#IndKecamatanId').on('change', function () {
                    table.DataTable().draw();
                });

                $('#IndKelurahanId').on('change', function () {
                    table.DataTable().draw();
                });

                $('#status').on('change', function () {
                    table.DataTable().draw();
                });

                $("#search").on('keyup', function () {
                    table.dataTable().fnFilter(this.value);
                });

                $('.dataTables_scrollBody').css('min-height', '150px');
            };

            return {

                //main function to initiate the module
                init: function () {
                    initTable1();
                },

            };

        }();

        jQuery(document).ready(function () {
            DatatablesAjaxServer.init();
        });

        $(function () {
            $("#IndKecamatanId").on("changed.bs.select", function () {
                if (this.value == " ") {
                    $("#IndKelurahanId option").remove();
                    $("#IndKelurahanId").append('<option value=" ">Semua</option>');
                    $('#IndKelurahanId').val(" ").selectpicker('refresh');
                    $("select #IndKelurahanId").attr("disabled", "");
                }
                else {
                    Drop_Kelurahan(this.value);
                }
            });
        });

        // LOAD DROPDOWN KELURAHAN+
        function Drop_Kelurahan(id) {
            ShowLoading();
            $("#IndKelurahanId option").remove();
            var sel = $("#IndKelurahanId");
            $.ajax({
                type: 'POST',
                url: "/AjaxData/DropKelurahan",
                dataType: 'json',
                data: { id },
                success: function (data) {
                    sel.removeAttr("disabled")
                    sel.append('<option value=" ">Semua</option>');
                    $.each(data, function (i, item) {
                        var option =
                            '<option value="' + item.id + '">' + item.uraian + '</option>';
                        sel.append(option);
                    });
                    $('#IndKelurahanId').selectpicker('refresh');
                },
                complete: function () {
                    $.unblockUI();
                }
            });
            return false;
        }

    </script>
}


